<% content_for :page_heading, "Notes" %>
<% content_for :page_actions do %>
  <%= link_to "New Note", new_note_path, class: "btn btn-sm btn-info" %>
<% end %>

<% @notes.in_groups_of(4).each do |group| %>
<div class="row">
  <% group.compact.each do |note| %>
  <div class="col-md-3">
    <div class="note panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= note.title %></h3>
      </div>
      <div class="panel-body">
        <%= simple_format note.body %>
      </div>
      <div class="panel-footer">
        <div>
          <%= link_to 'Show', note, class: "btn btn-xs btn-primary" %>
          <%= link_to 'Edit', edit_note_path(note), class: "btn btn-xs btn-warning" %>
          <%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-danger" %>
        </div>
      </div>
    </div>
  </div>
  <% end %>
</div>
<% end %>



<br>

